query classes($queryParams: ClassQueryParams!) {
  classes(queryParams: $queryParams) {
    id
    name
    method
    schedule {
      dayOfWeek
      startTime
      endTime
    }
    startDate
    endDate
    occupiedSlots
    totalSlots
  }
}
