query grades($queryParams: QueryParams!) {
  grades(queryParams: $queryParams) {
    meta {
      itemCount
      totalItems
      itemsPerPage
      totalPages
      currentPage
    }
    items {
      id
      name
      createdAt
      updatedAt
    }
  }
}
